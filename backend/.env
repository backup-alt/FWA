# MongoDB connection (Atlas cloud)
MONGODB_URI=mongodb+srv://universeexplorer4_db_user:VrAJcf7MJqgcTejr@cluster0.ftabrot.mongodb.net/vehicleFinanceDB?retryWrites=true&w=majority&appName=Cluster0

# JWT secret for auth tokens (change this in production!)
JWT_SECRET=change_this_to_a_long_random_secret_string

# Server port
PORT=5000

# Token expiry
JWT_EXPIRES_IN=7d

# Temporary secret for triggering one-off admin tasks (e.g. migration).
# Set the same value on Render's environment variables.
ADMIN_MIGRATION_SECRET=local_dev_temp_secret_change_me_in_prod